import React, { Component } from "react";
import { Query } from "react-apollo";
// import { GET_NOTES } from "./querys";

class App extends Component {
  render() {
    return (
      <div className="App">
        {/* <Query query={GET_NOTES}>{() => null}</Query> */}
      </div>
    );
  }
}

export default App;
